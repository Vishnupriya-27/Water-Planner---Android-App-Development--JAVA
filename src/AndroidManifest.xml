<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hidr8">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="WaterPlanner"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".MoreInformation"></activity>
        <activity android:name=".Settings" />
        <activity android:name=".WeeklyReport" />
        <activity android:name=".signupActivity" />
        <activity android:name=".Gender" />
        <activity android:name=".wokeup" />
        <activity android:name=".wokeup_g" />
        <activity android:name=".bedtime_boy" />
        <activity android:name=".bedtime_girl" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".homepage" />
        <activity android:name=".logo" />
        <activity android:name=".startpage" />
        <activity android:name=".Alarm"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".AlarmReceiver"/>

    </application>

</manifest>